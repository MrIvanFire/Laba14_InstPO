Что сделали:
1. JWT-авторизация

Регистрация и логин с хэшированием пароля

Генерация токена при успешном входе

2. Защита маршрутов

POST/PUT/DELETE /api/tasks требуют Bearer токен

Без токена → 401

3. Валидация

Проверка полей (Title, Priority, DueDate и др.)

Ошибки → 400 с JSON-списком

4. Обработка ошибок

Централизованный try-catch

Любое исключение → 500 с JSON

5. Git

Ветка feature/jwt-auth

Push, граф коммитов, stash

Что дало:
API защищён от неавторизованного доступа

Единый формат ошибок (400/401/404/500)

Сервер не падает при исключениях

Вручную всё реализовали (без ASP.NET Core middleware)
